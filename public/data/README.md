# 数据文件

将以下文件放入此目录，以支持题目与词库功能及 PWA 离线访问：

- `questions_v2.json` — 题目列表（含 related_terms）
- `glossary.json` — 术语百科

可从 `AWS-SAA` 项目目录复制：

```bash
cp /path/to/AWS-SAA/questions_v2.json /path/to/exam-app/public/data/
cp /path/to/AWS-SAA/glossary.json /path/to/exam-app/public/data/
```
