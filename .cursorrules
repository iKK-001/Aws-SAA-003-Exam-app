# AWS SAA-C03 备考 App — 全局行为准则

## 项目背景

AWS SAA-C03 备考移动端 Web App。支持按题练习、按分类刷题、百科词库、错题/收藏、术语抽认卡、随机 N 题再练、解析内术语可点、主题/音效、PWA 离线。

## 技术栈

- Next.js 14 (App Router)
- Tailwind CSS
- Lucide React
- next-pwa（PWA）
- 字体：Google Fonts Quicksand（next/font/google），作为默认 sans

## 核心数据结构

- **questions_v2.json**：题目列表。字段含 `id`, `question_cn`, `options_cn`, `best_answer`（单选为 string 如 `"A"`，多选可为 string 如 `"AB"` 或数组 `["A","B"]`）, `official_answer`, `tags`, `related_terms`, `explanation`（`analysis`, `why_correct`, `why_wrong`）。
- **glossary.json**：术语百科。键为术语名，值为 `definition`、`analogy`、`features` 数组。

## 设计原则

- **Mobile-First**：所有组件优先适配手机；底部导航栏；名词解释与设置使用自底向上抽屉 (Drawer)。
- **沉浸式学习**：解析渐进展示（先选答案再出解析）；按分类时以上位概念合并（如 ALB 相关归为 ALB），按题数从多到少排序。
- **可爱、有趣、游戏化**：整体风格偏轻松，避免「官方/严肃」感。圆角偏大（2xl/3xl）、柔和彩色阴影、Quicksand 字体；题目卡片厚底边像实体按键；选项有按下感（active scale + 深边框）；答对/答错有文案反馈（太棒啦 / 没关系）；角落 mascot 按状态变化（🤖/🥳/🧐/🎉/🤗）；今日已练 X 题 + 随机鼓励语；里程碑（10 题/50 题）只弹一次；术语抽屉突出「记住：XXX 就像 …」类比。
- **色彩与反馈**：正确用绿色系（emerald），错误用温和琥珀/红，避免过度「标红」；解析区标题口语化（「✅ 原来如此」「💡 记住这点」）。
- **主题与音效**：主题（如轻松/专注）只换主色和少量背景，不改变布局；音效默认关闭，极短提示音，在判对/错并展示解析前若开关打开则播放。

## 多选与判分

- 多选识别：题干含「选二」「选择两个」「Select TWO」等时按多选处理（lib/data.ts 从题干推断）；数据若有 `is_multiple`/`answer_count` 或 `best_answer` 为多字母串（如 `"AB"`）也按多选。
- `getBestAnswerArray(q)`：兼容 `best_answer` 为 string（如 `"AB"`）时拆成 `["A","B"]`。
- 多选需选够 `answer_count` 才判分、展示解析；判分规则：`userAnswers.sort().join('') === bestAnswer.sort().join('')`。

## 进度与恢复

- 顺序/按分类刷题时，从上次做到的位置自动继续（用 `getPracticeState` 的 order/topic 存 index+total）；无需弹窗，直接跳到上次题号。
- 题号旁有「从第一题开始」入口（index > 0 时显示），可清空该列表的保存进度并回到第 1 题。

## 内容与记忆（可复用）

- **练习页 sample 参数**：URL 支持 `?filter=wrong|favorite&mode=order&sample=N`。在得到 base（错题/收藏/全部等）后，若存在 `sample`，先 `shuffleArray(base).slice(0, Math.min(sample, base.length))` 再设 list；有 filter 无 mode 时默认 `mode=order` 以便从「我的」/首页直达练习。
- **抽认卡**：独立页（如 `/glossary/flashcard`）；随机取一条术语，正面只显示术语名，点击切换背面（definition + analogy）；「再抽一张」重新随机。百科与「我的」页提供入口。
- **解析内术语可点**：解析正文（analysis / why_correct / why_wrong）用 `HighlightTerms` 组件：输入 text、terms（`q.related_terms` 与 glossary 键合并去重）、onTermClick（打开术语抽屉）；按术语长度从长到短切分正文，术语渲染为可点击控件，点击即打开术语抽屉。

## 主题与音效（可复用）

- **主题**：lib/data.ts 提供 `getTheme()`/`setTheme('relaxed'|'focus')` 及 STORAGE_KEY；LayoutClient 挂载时 `document.documentElement.dataset.theme = getTheme()`；设置变更时调用 `setTheme()`（内部会写 localStorage 并设置 dataset.theme）；globals.css 用 `[data-theme="focus"]` 覆盖主色与少量背景（如 body、.bg-aws-slate-soft）；清除全部数据时重置 theme 并同步到 document。
- **音效**：lib/data.ts 提供 `getSoundEnabled()`/`setSoundEnabled(boolean)`，默认关闭；lib/sound.ts 用 Web Audio API 实现 `playCorrectSound()`/`playWrongSound()`（极短正弦波）；练习页在判对/错并 `setShowExplanation(true)` 前若 `getSoundEnabled()` 则调用对应音效；设置抽屉内「答对/答错音效」开关。

## 代码规范

- 使用 TypeScript。
- 组件保持原子化，逻辑与 UI 分离。题目卡片与选项交互集中在 `components/QuestionCard.tsx`；解析内术语高亮用 `components/HighlightTerms.tsx`。
- 数据与本地状态：题目/词库由 DataContext 拉取；进度、错题、收藏、收藏术语、考试日期、练习位置、今日题数、里程碑已弹、主题、音效存 localStorage（见 lib/data.ts）。
- 避免重复导入：同一文件内不要重复 `import Link from 'next/link'` 等同名符号。
